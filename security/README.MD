# passGen

A simple Bash script that generates passwords of varying lengths. You can also generate as many passwords as you like.


# Usage

chmod +x ./passGen.sh

./passGen.sh [Amount of symbols] [Amount of passwords]

Example: ./passgen.sh 14 6
